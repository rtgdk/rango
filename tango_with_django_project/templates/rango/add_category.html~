<!DOCTYPE html>
{% extends 'rango/base2.html' %}
<html>
	
	<head>
		{% block title %}Add Category{%endblock%}
	</head>
	<body>
		{% block body_block %}
		{% if user.is_authenticated %}
		
		<form role="form"  id="page_form" method="post" action="/rango/">
            <h2 class="form-signin-heading">Add a Category</h2>
			{% csrf_token %}
			{% for hidden in form.hidden_fields %}
				{{ hidden }}
			{% endfor %}
			{% for field in form.visible_fields %}
				{{ field.errors }}
				{{ field.help_text}}
				{{ field }}
			{% endfor %}
			<button class="btn btn-primary" type="submit" name="submit">Add category</button>
		</form>
		{%else %}
		<h2> Not a registered user.</h2>
		<h3>You need to be registered to Add categories.</h3>
		{%endif %}
		{% endblock %}
	</body>
</html>
