<!DOCTYPE html>
{% extends 'rango/base2.html' %}
<html>
	<head>
		{% block title %}{{ category_name }}-Add a page{% endblock %}
	</head>
	<body>
		{% block body_block %}
		{% if user.is_authenticated %}
		
		<form role="form"  id="page_form" method="post" action="/rango/category/{{ category_name_url}}/add_page/">
            <h2 class="form-signin-heading">Add a Page to <a href="/rango/category/{{category.slug}}/"> {{ category_name }}</a></h2>
                    {% csrf_token %}
                    {% for hidden in form.hidden_fields %}
                        {{ hidden }}
                    {% endfor %}

                    {% for field in form.visible_fields %}
                        {{ field.errors }}
                        {{ field.help_text }}<br/>
                        {{ field }}<br/>
                    {% endfor %}

                <br/>
            <button class="btn btn-primary" type="submit" name="submit">Add Page</button>
                </form>>
		{% else %}
		<h3>Not registered to add pages.</h3>
		{%endif%}
		{%endblock%}
	</body>
</html>
